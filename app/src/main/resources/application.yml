spring:
  application:
    name: chirp

  data:
    redis:
      host: redis-15202.c73.us-east-1-2.ec2.cloud.redislabs.com
      password: ${REDIS_PASSWORD}
      username: "default"
      port: 15202
      connect-timeout: 5000ms
      timeout: 2000ms

  datasource:
    url: jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres
    username: postgres.reaacwztaqjpnocrmsrt
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 30000
      max-lifetime: 12000000
      validation-timeout: 5000
      pool-name: SpringBootHikariCP
      data-source-properties:
        prepareThreshold: 0
        preparedStatementCacheQueries: 256
        preparedStatementCacheSizeMib: 5

        tcpKeepAlive: true
        socketTimeout: 30
        connectTimeout: 10
  jpa:
    properties:
      hibernate.default_schema: chat_service
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true

  mail:
    port: 587
    password: ${MAILGUN_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  rabbitmq:
    host: collie.lmq.cloudamqp.com
    password: ${RABBITMQ_PASSWORD}
    username: fjkjnibi
    ssl:
      enable: true
    virtual-host: fjkjnibi
    listener:
      simple:
        acknowledge-mode: auto
        concurrency: 1
        max-concurrency: 3
        prefetch: 10
        retry:
          enable: true
          initial-interval: 1000ms
          max-interval: 10000ms
          max-attempts: 3


jwp:
  secret: ${JWT_SECRET_BASE64}
  expiration-minutes: 15

chirp:
  email:
    from: pgsa73@sandboxf3e65bb776bc41caacf3257e54232862.mailgun.org
    verification:
      expiry-hours: 24
    reset-password:
      expiry-minutes: 30

nginx:
   trusted-ips:
     - "127.0.0.1" # nginx on same machine
     - "::1" # IPv6 localhost
     - "172.17.0.0/16" # nginx container IP when using Docker
     - "10.0.0.5/32" # nginx server IP (if separate machine)